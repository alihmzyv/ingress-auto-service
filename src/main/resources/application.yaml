spring:
  application:
    name: ingress-auto-service
  config:
    import:
      - classpath:datasource-properties.yaml
      - classpath:hibernate-properties.yaml
      - classpath:security-properties.yaml